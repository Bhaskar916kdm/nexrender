{
  "name": "nexrender-railway-server",
  "private": true,
  "version": "1.0.0",
  "description": "Nexrender server for Railway deployment",
  "license": "MIT",
  "scripts": {
    "start": "npx nexrender-server --port $PORT --secret $NEXRENDER_SECRET"
  },
  "dependencies": {
    "@nexrender/server": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
